spring:
  application:
    name: cnb-springboot

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
server:
  port: ${PORT:8080}

app:
  message: Hello Spring Boot @ ... after 1:37 PM PM !
  title: Spring Boot with Cloud-native Buildpacks