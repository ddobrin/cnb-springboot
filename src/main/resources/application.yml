management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
server:
  port: ${PORT:8080}

app:
  message: Hello Spring Boot - local demo with Tanzu Build Service - Another Commit --> Build!
  title: Spring Boot with Cloud-native Buildpacks
